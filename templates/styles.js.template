const useStyles = ReactJss.createUseStyles({
    sampleStyle: {
        position: "relative",
        background: "#ffffff",
        boxShadow: "0px 0px 1px rgba(0, 0, 0, 0.05), 0px 4px 16px rgba(0, 0, 0, 0.13)",
        borderRadius: "10px",
        display: "inline-block",
        padding: "10px",
    },
});

export default useStyles;
